import { Fragment, FunctionalComponent, h } from 'preact';

const SprintShowAndTell: FunctionalComponent = () => {
    return (
        <Fragment>
            <div className="create-bar">
                <h1 className="page-heading">Show and Tell</h1>
            </div>
        </Fragment>
    );
};

export default SprintShowAndTell;
